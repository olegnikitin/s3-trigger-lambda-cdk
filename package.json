{
  "name": "csv-processor-cdk",
  "version": "1.0.0",
  "bin": {
    "csv-processor-cdk": "bin/app.js"
  },
  "scripts": {
    "build": "tsc && cd lambda && npm install && npm run build",
    "watch": "tsc -w",
    "cdk": "cdk",
    "deploy": "npm run build && cdk deploy",
    "destroy": "cdk destroy",
    "synth": "cdk synth",
    "diff": "cdk diff"
  },
  "devDependencies": {
    "@types/node": "^20.11.16",
    "aws-cdk": "^2.126.0",
    "source-map-support": "^0.5.21",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.126.0",
    "constructs": "^10.3.0"
  }
}
